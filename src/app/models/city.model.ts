export interface City {
    id: string,
    name: string,
    date: string,
    duration: string,
    region: string,
    price: number,
   imageUrl : string,
    description: string,
    activities: string[]
}